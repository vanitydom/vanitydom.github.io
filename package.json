{
  "scripts": {
    "jsonlint": "( for i in [0-9][0-9].*.json ; do jsonlint -q $i || exit 1 ; done )"
  },
  "dependencies": {
    "fixed": "^0.3.1",
    "mustache": "^4.2.0",
    "outdent": "^0.8.0"
  },
  "devDependencies": {
    "jsonlint": "^1.6.3"
  }
}
